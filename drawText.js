function drawText(screenWidthInCharacters, screenHeightInCharacters) {
  var wholeScreenTextBuffer = "";
  // draw top bar
  var singleRow = "";
  for (let index = 0; index < screenWidthInCharacters; index++) {
    singleRow += "";
  }
}
